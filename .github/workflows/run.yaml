name: learn-github-actions
on: [push]
jobs:
  jobs-name:
    runs-on: ubuntu-latest
    steps:
      - run: echo "this is pen"
      - uses: actions/checkout@v4
      - run: |
          ls -al
          cd .github/workflows/
          ls -al
